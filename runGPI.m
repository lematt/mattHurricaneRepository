%This script is used to load the data necessary for running the genesis
%potential index algorithm


if ~exist('dataLoaded', 'var') || ~dataLoaded
    dataPath = '/project/expeditions/lem/data/';
    
    
    dataLoaded = true;
end